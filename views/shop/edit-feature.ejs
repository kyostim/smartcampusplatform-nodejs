<%- include('../includes/head.ejs') %>
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/thing.css">
</head>

<body>
   <%- include('../includes/navigation.ejs') %>

    <main>
        <form class="thing-form" action="/admin/<% if (editing) { %>edit-feature<% } else { %>add-features<% } %>" method="POST">
            <div class="form-control">
                <label for="name">name</label>
                <input type="text" name="name" id="name" value="<% if (editing) { %><%= feature.name %><% } %>">
            </div>
            <% if (editing) { %>
                <input type="hidden" value="<%= feature.id %>" name="thingId">
            <% } %>
<button class="btn" type="submit"><% if (editing) { %>Update Feature<% } else { %>Add Feature<% } %></button><!DOCTYPE html>
            <html lang="en">
            <head>
                <meta charset="UTF-8">
                <meta http-equiv="X-UA-Compatible" content="IE=edge">
                <meta name="viewport" content="width=device-width, initial-scale=1.0">
                <title>Document</title>
            </head>
            <body>
                
            </body>
            </html>--
        </form>
    </main>
<%- include('../includes/end.ejs') %>